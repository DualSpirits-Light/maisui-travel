# 麦穗旅序 0.2.0 验证记录

验证日期：2026-09-08。

## 构建与安装

- 使用 Android SDK 35、Build Tools 35.0.0、JDK 21 和 Java 17 源语言级别完成独立构建。
- APK 包名为 `cn.lvxu.travel`，`versionCode` 为 3，`versionName` 为 0.2.0。
- APK v2 / v3 签名校验通过，原生库按 16 KiB 页面要求对齐。
- Android 15 / API 35 x86_64 模拟器安装、启动与覆盖升级成功，运行日志未发现应用崩溃。
- 先安装 0.1.1 并创建杭州示例，再覆盖安装 0.2.0；原旅行和 6 个地点均保留，首次教程按新版本规则显示。

## 自动测试

- 模型测试：23 项断言通过，覆盖金额精度、时间和日期校验、排序、分类迁移、清单属性、版本化 JSON 往返等。
- 地图与地点导入测试：13 项断言通过，覆盖坐标解析、直线距离、高德分享内容提取及异常输入。
- Android 独立仪器测试：21 项断言通过，覆盖 SQLite 持久化、大数据读取、ZIP 合并恢复、媒体文件、路径穿越和损坏备份拒绝、离线授权码的有效/篡改/过期分支，以及 WebDAV 的连接、上传、列出、下载、错误密码回滚。
- WebDAV 使用本机 TLS 测试服务和固定测试证书验证，全链路通过 OkHttp 实际发送 `PROPFIND`、`PUT` 与 `GET`，未用模拟返回替代网络传输。

## 模拟器功能验证

- 七步新手教程可创建杭州示例；四步快速规划可生成新旅行。
- “我的旅行”标题点击进入、长按删除确认正常。
- 预算分类新增、修改、删除与账单迁移正常；支出日期时间、饼图和折线图切换正常。
- 多清单、自定义属性、重要性和图片入口正常；至少保留一个清单的保护逻辑正常。
- 设置抽屉、头像昵称、背景图、主题与底栏排序/显隐入口正常。
- 高德隐私同意前不初始化 SDK；同意后原生高德地图成功显示编号标记、折线和距离。杭州“West lake”在线 POI 搜索返回 15 条真实结果。
- 打卡照片可生成带地点、时间和心情的电子卡片预览，并成功写入系统 `Pictures/麦穗旅序/` 相册目录。

## 验证范围

尚未在实体 ARM 手机及 Android 8–14 / 16–17 的完整设备矩阵逐一测试，也未连接用户实际使用的 WebDAV 服务。私有 GitHub 仓库无法匿名读取 Release，因此更新模块只完成本地逻辑与安全校验，未做私有 Release 的端到端下载。地图动线为地点间直线，不是道路导航路线。Android Studio / Gradle 构建入口已配置，本轮最终 APK 使用项目附带的独立构建脚本生成。

当前 APK 使用开发测试签名，适合直接试用和覆盖 0.1.x 测试版；应用商店发布前应改用由发行方保管的正式签名并完成真机与隐私合规测试。

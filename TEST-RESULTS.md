# 麦穗旅序 0.2.5 验证记录

验证日期：2026-09-10。versionCode 8，最低 API 26，目标 API 35。

- 最终 APK 构建成功，v2/v3 签名通过。
- 35 项模型、21 项导入、14 项云授权签名断言通过。
- 89 项 Android 集成断言通过，覆盖加密凭据存取、备份排除授权、错误设备/产品/签名/过期拒绝、网络故障保留凭证、401/403 清除凭证，以及原有界面回归。
- 10 项服务端测试通过，无跳过，包括并发设备数量限制。
- 线上实际验证生成、激活、签名验签、七天凭证、第二设备拒绝、刷新、解绑换机和吊销。测试授权已吊销。
- 最终源代码审查未发现阻止发布的问题。

边界：仅验证 API 35 模拟器；完全离线的凭证在管理员解绑或吊销后最迟七天失效。重复激活若响应丢失，原设备凭据可能失效，重新输入原授权码可恢复。管理员令牌、私钥及自用授权码不进入公开源码和安装包。

---
# 麦穗旅序 0.2.4 验证记录

验证日期：2026-09-09。包名 cn.lvxu.travel，versionCode 7，最低 API 26，目标 API 35。

## 本轮验证

- 最终 APK 独立构建成功，v2/v3 签名验证通过，沿用原签名覆盖安装。
- 35 项模型断言通过：新增自定义排序、数据往返、非法排序拒绝与时间保留。
- 21 项地图及导入断言通过：新增收藏夹链接识别和文件夹标识提取。
- 60 项 Android 集成与界面断言通过，包括原有数据库、备份和分享验证；原图字节/长宽比/颜色保留，全屏 FIT_CENTER 与无变暗层，关闭按钮；裁剪放弃不替换、保存新方图且原文件不变；实际触控长按拖拽换序及持久化、地点和时间不被删除修改；轻滑展开、返回先关闭；配置全屏、标题居中、圆形头像、归档入口及退出动画。
- 模拟器手动验证快速规划输入：点击后实际显示输入法，可以输入文字，底部取消/下一步仍位于键盘上方。
- 用户提供的高德收藏夹公开分享页可访问并识别收藏夹标识，但页面及其公开脚本只提供唤起 App 的跳转，未提供 10 个地点、备注和照片的列表。本次批量导入测试失败；应用明确提示改用逐个地点分享链接，不伪造导入成功。

## 边界

覆盖 API 35 模拟器，未覆盖所有品牌真机。方形缩略图只裁切展示区域，新选照片文件保持原始内容；旧版本已裁掉的像素需要重新选择原图恢复。拖动改变显示顺序，保留已填写时间。WebDAV 本轮未连接用户服务器。

---

# 麦穗旅序 0.2.3 验证记录

验证日期：2026-09-08 至 2026-09-09。包名 cn.lvxu.travel，versionCode 6，最低 API 26，目标 API 35。

## 本轮验证

- 独立构建成功，APK v2/v3 签名校验通过，沿用原签名覆盖安装。
- 30 项模型测试通过：包括新增旅行资料往返、旧版字段默认值、媒体路径限制。
- 19 项地图和分享解析测试通过。
- 35 项 Android 集成与界面断言通过：SQLite、备份合并与损坏保护、媒体恢复、自定义口号备份、单旅行 ZIP 预览、独立 ID 与照片路径、损坏分享文件不改变现有旅行，以及快速规划窗口身份复用、前后按钮顺序、日期弹窗、草稿保留和取消关闭。
- 模拟器实际操作通过：自定义出行方式添加并选中，垃圾桶二次确认，取消删除保留类别，确认删除移除类别；首页口号编辑保存与恢复默认。
- 快速规划住宿链接入口使用现有高德样本链接验证，回填伊兴面片(五泉山店)及火车站西路788号。样本是地点链接解析测试，不代表推荐该餐馆作为住宿。
- 从行程页生成含实际照片的旅行 ZIP，系统分享面板正常；选回本 App 后显示正确导入预览。系统活动记录确认仅一个 MainActivity，外部分享使用 singleTask/onNewIntent，避免旧实例覆盖导入数据。
- 最终审查发现并修复外部文件可能新建独立页面实例的风险，修复后重跑 Android 测试通过。

## 边界

分享双方需使用 0.2.3 或更新版本。文件包含该旅行的备注、同行人、费用、清单、打卡与引用照片；生成前显示内容说明，由用户选择发送方式，不包含应用设置、账号或激活信息。分享文件上限 128 MB、单文件上限 32 MB、最多 1000 个条目；导入新旅行，不覆盖原行程。

高德信息以分享链接实际提供内容为准；自定义住宿文字始终可手动填写。未覆盖所有安卓版本与品牌真机，未对第三方聊天软件传输文件逐一验证。WebDAV 沿用原实现，本轮没有连接用户服务器。

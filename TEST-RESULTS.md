# 旅序 0.1.0 验证记录

验证日期：2026-09-06。

## 构建与安装

- 官方 Android SDK 35、Build Tools 35.0.0，JBR JDK 21.0.7，Java 源语言级别 17。
- 使用工程附带 `build-apk.ps1` 编译成功；APK v2 / v3 签名校验通过。
- Android 15 / API 35 x86_64 模拟器，1080 × 2400、420 dpi：首次安装及最终版本覆盖安装均成功。
- 运行日志未发现应用 AndroidRuntime 崩溃。

## 模型测试

17 项断言全部通过：人民币分精度、显示格式、金额上下限、负数 / 超过两位小数 / 非数字拒绝、按天筛选、按时间排序、实际和预计费用分离、JSON 往返、清单状态保留，以及非法天数 / 地点日期 / 预算 / 时间格式拒绝。

## 模拟器交互

- 新建 Weekend / Hangzhou 旅行成功。
- 为第 1 天添加 WestLake 地点成功。
- 添加 Lunch 实际支出 28.50 元，汇总显示 28.5 元。
- 勾选身份证清单，完成数更新为 1 / 2。
- 强制关闭应用并重启，旅行、地点、费用和清单勾选状态保留。
- 最终版本覆盖安装后原数据保留。
- Android 系统文件选择器导出 JSON 成功；读取文件确认两段旅行、账单金额 2850 分。
- 通过系统选择器导入该备份后，旅行数从 2 增加到 4，原旅行保留。
- 检查首页与预算页截图后修复底部导航居中；最终页面截图随交付提供。

## 验证范围限制

未在实体手机或 Android 8–14 / 16–17 逐一测试。项目声明最低支持 API 26，但不代表所有品牌系统均已验证。未实测第三方地图应用、外部分享目标、磁盘写满或损坏文件的完整恢复流程。Gradle/Android Studio 构建入口已提供，此次实际使用独立 SDK 脚本构建，没有声称 Gradle 构建已验证。

该包使用开发测试签名，不是应用商店发布版本。

## 0.1.1 名称更新验证

应用标签、首页、关于和备份前缀改为麦穗旅序；同一密钥重新编译并通过 APK v2/v3 签名验证。AAPT 检查包名仍为 cn.lvxu.travel，versionCode 为 2。此更名版本未重复执行模拟器回归；本轮重构方案中的新功能尚未集成。

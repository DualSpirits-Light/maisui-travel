# 麦穗旅序重构路线

当前可运行版本为 0.1.1：原生 Java、离线 JSON 存储，已完成正式改名。以下为后续规划，尚未全部实现。

## 基础系统与配置

- 迁移到 Kotlin、Compose、ViewModel/StateFlow、Room 和统一 Repository。
- 注册、登录、邮箱验证、密码重置、退出及注销账号。服务端验证会话；刷新令牌加密保存，网络错误不直接清空登录状态；账号数据隔离。
- WebDAV 版本化备份：业务数据一致性快照、图片附件、版本清单与哈希；排除所有账号凭据。恢复先校验再导入，保留原数据。

## UI 与交互

- 自定义背景：Photo Picker / 文件选择器、裁剪、私有附件存储；可选 Unsplash 随机图片和来源署名。
- 统一旅行列表组件：长按确认删除，点击标题进入详情。
- 类型安全路由，只传稳定 ID，支持进程重建与已删除行程处理。

## 行程与财务

- 自定义费用类别的增删改查，删除类别时在事务内迁移账单，金额继续按分存储。
- 多个独立清单，重要性、自定义字段、多个图片附件和完成状态。

## 地图与打卡

- 系统拍照、按需定位、逆地理编码、心情描述及 Canvas 电子卡片，支持保存。
- 列表与地图共享每日行程状态，地图显示地点和动线。
- 明确区分经纬度直线距离与路线服务返回的道路距离，并标识坐标系。

## 迁移与验收

1. 保留原始 trips-v1.json；在 Room 事务内导入并写迁移标记，核对旅行数、费用总额和清单状态。
2. 验证账号切换、并发续期、离线启动、退出后的状态。
3. 验证备份中断、错误密码、冲突、附件缺失、跨版本恢复。
4. 验证权限拒绝、图片旋转和大图内存、定位超时、地图生命周期与真机兼容。

构建工具及第三方依赖在重构分支统一锁定并编译验证。地图 Key、图片 API 凭据和账号服务配置通过外部配置提供，禁止把真实服务端密钥或用户 WebDAV 密码提交到仓库。
